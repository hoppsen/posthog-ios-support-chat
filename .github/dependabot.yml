version: 2
updates:
  # The package itself has no dependencies; this keeps the workflow actions
  # (checkout, cache, …) from going stale.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    commit-message:
      prefix: CI
